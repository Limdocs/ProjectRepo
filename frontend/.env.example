# Copy to .env.local and fill in values (Vite exposes only VITE_* to the client).
# From SAM/CloudFormation outputs: UserPoolId, UserPoolClientId, ApiUrl.
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
# Include the API Gateway stage path (e.g. ...amazonaws.com/prod).
VITE_API_URL=
